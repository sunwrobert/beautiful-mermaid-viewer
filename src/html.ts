import { html } from 'foldkit/html'

import type { Message } from './main'

export const {
  button,
  div,
  empty,
  h1,
  span,
  textarea,
  Class,
  InnerHTML,
  OnClick,
  OnInput,
  OnPointerDown,
  OnPointerMove,
  OnPointerUp,
  Placeholder,
  Style,
  Value,
} = html<Message>()
